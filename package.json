{
  "name": "fnkit",
  "version": "0.7.6",
  "description": "Functions as a Service CLI - scaffold and deploy serverless functions without the netes",
  "module": "src/index.ts",
  "type": "module",
  "bin": {
    "fnkit": "./dist/fnkit"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build ./src/index.ts --compile --outfile ./dist/fnkit",
    "build:all": "bun run build:macos && bun run build:linux && bun run build:windows",
    "build:macos": "bun build ./src/index.ts --compile --target=bun-darwin-arm64 --outfile ./dist/fnkit-macos-arm64",
    "build:linux": "bun build ./src/index.ts --compile --target=bun-linux-x64 --outfile ./dist/fnkit-linux-x64",
    "build:windows": "bun build ./src/index.ts --compile --target=bun-windows-x64 --outfile ./dist/fnkit-windows-x64.exe",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "keywords": [
    "fnkit",
    "functions",
    "serverless",
    "google-cloud",
    "functions-framework",
    "cli"
  ],
  "author": "",
  "license": "MIT"
}